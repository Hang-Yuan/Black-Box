# Black Box v0.14.74

## 修复

- CLI 进程意外退出后，不再把 `pendingTurnInput` 当作未发送草稿重新放回输入框。
- 避免已经进入 Claude JSONL 的用户消息在恢复时被再次发送，造成重复气泡、重复工具调用和重复执行。
- 恢复提示准确区分已保留的会话与可证实未发送的输入。

## Validation

- Health recovery finalizes the lost process without restoring an already-sent turn.
- Explicit Stop continues to restore the interrupted input and attachments for intentional retry.
- Existing live-transcript reconciliation and managed-process recovery tests remain green.
