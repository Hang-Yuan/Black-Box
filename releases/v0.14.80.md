# Black Box v0.14.80

## 修复

- API 配置的具体模型名输入框恢复完整可用宽度，可直接手动填写 `claude-opus-5-5` 等模型 ID。
- 上下文容量改为独立固定宽度，不再挤压模型名；官方 Claude 型号仍自动采用对应的最大上下文。
- 修改映射后，顶部模型选择器继续显示并切换该具体模型名称。

## Validation

- TypeScript 类型检查与生产构建通过。
- Vitest 128 个测试文件、991 项测试通过。
- 新增模型名宽度与上下文窄栏的布局回归测试。
