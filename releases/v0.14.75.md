# Black Box v0.14.75

## 修复

- 会话收到终态 `result` 后，主动与 Claude JSONL 做一次完整对账，并用有限重试覆盖文件落盘顺序竞态。
- 回复中的裸文件名保留该回复生成时的最深工作目录，点击后可解析到实际文件。
- 普通消息更新也统一保留更具体的 `cwd`，避免后到的项目根目录快照覆盖已知深层目录。

## Validation

- Added a terminal-state regression that starts with a completed turn carrying the workspace root and verifies durable JSONL repairs it to the nested file directory without adding a duplicate message.
- All 984 frontend tests pass across 128 files.
- TypeScript compilation passes.
