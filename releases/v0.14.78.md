# Black Box v0.14.78

## 修复

- 文件引用从消息产生时的运行目录逐级回溯到工作区边界，支持 Claude 在深层运行目录中输出项目根相对路径。
- 裸文件名先在最近项目范围搜索，避免大型工作区全局扫描截断后误报不存在。
- 多段路径至少要求目录后缀匹配，不会打开其他目录里的同名文件。
- 文件夹引用直接验证并定位；尚未生成的目标文件保留明确的“找不到文件”提示。

## Validation

- TypeScript 类型检查通过。
- 文件引用单元测试 61 项通过。
- 原生 WebView 点击回归 6 项通过，覆盖嵌套 cwd、项目相对目录、裸文件名、关闭后重开与不存在文件。
