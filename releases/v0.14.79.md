# Black Box v0.14.79

## 修复

- Claude 将原始图片转存并生成高清切片后，持久消息会绑定回发送时的本地气泡，不再显示第二条相同用户消息。
- 图片切片读取说明不再被当成文件路径；历史记录只展示真实的原图和切片附件。
- 连续发送相同文本时，每条持久消息只认领一个本地气泡，避免错误合并。

## Validation

- TypeScript 类型检查通过。
- 消息同步与会话加载定向测试 31 项通过。
- 真实图片消息结构回归通过：单气泡、持久 UUID 绑定、原始预览保留、说明行过滤。
