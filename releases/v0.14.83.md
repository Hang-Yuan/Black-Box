# Black Box v0.14.83

## 修复

- 官方原生 Claude CLI 的大文件升级改为分段下载；连接超时后再次点击更新会复用已完成的分段，不必重新下载全部文件。
- 根据官方发布清单校验压缩包和解压后的可执行文件，并在候选版本通过 `--version` 检查后切换 CLI。失败时继续使用原有版本。
- 当前仅覆盖 macOS 官方原生安装布局；其他安装方式继续使用各自的升级程序。

## 验证

- Rust 单元测试覆盖断点续传、分段边界、双重哈希校验、候选版本核验及原子切换。
- 完整 Rust 测试、TypeScript 检查和前端构建通过。
