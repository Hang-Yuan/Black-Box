# Black Box v0.14.76

## 修复

- 按 Codex 的 citation preparation 思路，在渲染消息时一次性提取同一回复里明确写出的落盘目录。
- 历史缓存只保留工作区根目录时，裸文件名优先使用回复内的落盘目录，不再依赖可能被大型工作区截断的全盘搜索。
- 文件引用最终无法解析时显示错误提示，消除“点击无反应”的静默失败。

## Validation

- 新增原生 WebView 点击验收：点击 `skill-layout.md` 后断言目标文件、文件内容与右侧预览均已载入。
- 覆盖关闭预览后再次点击同一文件。
- 覆盖缓存 cwd 退化到工作区根目录且全盘搜索被强制截断的历史消息，仍从同一回复声明的落盘目录打开目标文件。
